"""Bundle-format primitives for Layer 1.

The implementation is introduced in T02. Keeping this module explicit from
T01 makes the producer, consumer and security-boundary tests discoverable
without pre-implementing the cryptographic format.
"""
